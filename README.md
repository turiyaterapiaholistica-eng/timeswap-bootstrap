# TimeSwap Bootstrap

Repositorio minimo para publicar o arquivo `timeswap-session.json`.

Uso:

- `timeswap live` atualiza esse arquivo
- o APK ou app web fixo le esse JSON por uma URL publica estavel

Arquivo principal:

- `timeswap-session.json`
